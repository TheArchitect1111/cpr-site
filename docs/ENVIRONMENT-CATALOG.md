# Environment Catalog

The environment catalog defines variables that must be known before release.

## Staging Access

```text
CPR_STAGING_OPEN
NEXT_PUBLIC_CPR_STAGING_OPEN
```

These flags control whether CPR staging access is intentionally open for validation. They must be reviewed before production promotion.

